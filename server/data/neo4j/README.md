# Neo4j Placeholder

This folder is reserved for future Neo4j drivers, graph queries, and repositories.

Add Neo4j code only when a feature needs graph relationships such as bar similarity,
neighborhood links, metro proximity, recommendation paths, or themed routes.
